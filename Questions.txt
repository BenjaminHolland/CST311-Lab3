a. The orignal hosts were on the same network, and the router did not have the required routing information.
b. yes
c. Changing the subnet mask of h2's IP address from 24 to 16 causes routing to fail, because h2 is no longer on the same network as the router interface it's connected to.
